var accordionItem = document.querySelectorAll(".accordion-item");

accordionItem.forEach((element) => {
  element.classList.add("border");
  element.classList.add("border-seconday");
  element.classList.add("rounded");
});
